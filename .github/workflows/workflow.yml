name: hello world
on: push
jobs: 
 build: 
  runs-on: ubuntu-latest
  steps: 
  - name: build step
    run: echo "demo build"
 test:
   runs-on: ubuntu-latest
   needs: build
   steps:
    - name: test step
      run: echo "test demo"
